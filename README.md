# Nintendo DS C++ Homebrew Template

Usage: `copier copy gh:arylen/nds-cpp-template ./my-homebrew`

Note: Only MacOS is supported, just a personal template atm.

Assumes that devkitPro is already set up.

Assumes that melonDS is installed at /Applications/melonDS.app
